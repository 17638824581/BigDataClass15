package com.dahua.collection.list;

public class StackAndQueue {

    public static void main(String[] args) {
        // 栈和队列结构

        // 括号匹配
        // 可以使用栈来解决
        // {{{()[]}()}{{[]}}}}

        // 1. 从左到右遍历括号
        // 2. 如果遇到左括号，就入栈
        // 3. 如果遇到右括号，就出栈
        // 4. 判断这两个括号匹不匹配

        // 计算四则元素上

        // 带码运行的时候，方法调用，

        // 网站中的上一页功能。

        // 栈结构：先进后出，后进先出。
        //  存入到栈中的数据，先存进去的会被存到最里面去，取出数据的时候，从外面取。
        // 使用栈的时候，需要使用它特有的方法，
        // 1. push() 入栈：往栈中压入一个元素。
        // 2. pop() 出栈：从栈中弹出一个元素。
        // 3. empty() 判断栈是否为空。
        // 4. peek() 查看当前栈顶的元素（不会弹出元素）。
        // 5. serach() 查看指定元素在栈中的位置。栈顶是1。

//        Stack<String> s = new Stack<>();
//
//        s.push("小明");
//        s.push("小王");
//        s.push("小东");
//        s.push("小黑");
//
//        int index = s.search("小东");
//
//        System.out.println(s);
//        System.out.println(index);

/*        System.out.println("s.peek() = " + s.peek());

        String s1 = s.pop();
        String s2 = s.pop();
        String s3 = s.pop();
        String s4 = s.pop();

        System.out.println(s1);
        System.out.println(s2);
        System.out.println(s3);
        System.out.println(s4);*/

        // 队列：先进先出，后进后出。
    }
}
