<?xml version="1.0" encoding="UTF-8" ?>
<!-- Mybatis的核心配置文件 -->
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">

<configuration>
    <!-- 引入外部配置文件 -->
    <properties resource="jdbc.properties"/>

    <!-- 配置类路径别名 -->
    <typeAliases>
        <package name="com.xty.pojo"/>
    </typeAliases>

    <!-- 配置自定义的类型处理器 -->
    <typeHandlers>
        <typeHandler handler="com.xty.typeHandler.DateTypeHandler" javaType="java.util.Date" jdbcType="VARCHAR"/>
    </typeHandlers>

    <!-- 配置插件 -->
    <plugins>
        <!-- 分页助手插件 -->
        <plugin interceptor="com.github.pagehelper.PageHelper">
            <property name="dialect" value="mysql"/>
        </plugin>
    </plugins>

    <environments default="development">

        <!-- 配置环境 -->
        <environment id="development">
            <transactionManager type="JDBC"/>
            <!-- 配置数据源 -->
            <dataSource type="POOLED">
                <property name="driver" value="${driver}"/>
                <property name="url" value="${url}"/>
                <property name="username" value="${username}"/>
                <property name="password" value="${password}"/>
            </dataSource>
        </environment>

    </environments>


    <!-- 配置 Sql映射xml文件 -->
    <mappers>
        <mapper class="com.xty.mapper.BookMapper"/>
        <mapper class="com.xty.mapper.StudentMapper"/>
    </mappers>

</configuration>